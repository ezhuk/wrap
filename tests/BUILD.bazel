load("@rules_cc//cc:defs.bzl", "cc_test")

package(
    default_package_metadata = ["//:license"],
)

cc_test(
    name = "wrap_test",
    srcs = ["wrap_test.cpp"],
    deps = [
        "//:wrap",
        "@cpp-httplib//:httplib",
        "@googletest//:gtest_main",
    ],
)
