load("@rules_cc//cc:defs.bzl", "cc_binary")

package(
    default_package_metadata = ["//:license"],
)

cc_binary(
    name = "wrap-router",
    srcs = ["main.cpp"],
    deps = [
        "//:wrap",
        "@fmt",
    ],
)
