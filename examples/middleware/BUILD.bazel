load("@rules_cc//cc:defs.bzl", "cc_binary")

package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "wrap-middleware",
    srcs = ["main.cpp"],
    deps = [
        "//:wrap",
        "@fmt",
    ],
)
